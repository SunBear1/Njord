name: Setup Node.js environment
description: Install Node.js 22 and npm dependencies (requires prior actions/checkout)

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: "22"

    - run: npm ci
      shell: bash
