# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backtest-metrics.json
.wrangler/
.dev.vars

# Terraform (kept for k3s/Helm work; no .tf files committed)
infrastructure/.terraform/
infrastructure/*.tfvars
infrastructure/*.tfstate
infrastructure/*.tfstate.backup
infrastructure/*.tfplan
infrastructure/crash.log
infrastructure/.terraform.lock.hcl

# Stray files
test-results/
Prompt_*.md
*.Zone.Identifier
screenshots/
.env
.env.*
!.env.example
Njord_UI_Redesign_Plan.md
Njord_WCAG_AAA_Guardrails_Addendum.md
aurora-topbar-website*
aurora-visualization*
best-aurora-ever*
*.Zone.Identifier
Aurora Borealis Animation.jsx
Aurora Borealis Animation.jsx:Zone.Identifier

# ICM (persistent memory) - local session state
.copilot/

# GitHub hooks (if using local hooks instead of repo-based hooks)
.github/hooks/rtk-rewrite.js

# Claude Code local/personal settings (agents + project CLAUDE.md are checked in)
.claude/settings.local.json

# BMAD local tool install (vendored skills, not project config) + local-only AGENTS.md
AGENTS.md
.agents/

# BMAD user-scoped config
_bmad/**/*.user.toml
